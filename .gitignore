/.bundle/
/.yardoc
/_yardoc/
/coverage/
/doc/
/pkg/
/spec/reports/
/tmp/
/spec/examples.txt

# Bundler
Gemfile.lock

# RubyGems
*.gem

# Local notes — not for publication
/docs/

# Native binaries — downloaded by `rake update_binaries`, not committed.
# `rake build` regenerates them before packaging the gem.
/libs/*/libc2pa_c.*
